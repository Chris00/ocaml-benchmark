#!/bin/sh

if [ -z "`which ocamlfind.opt`" ]; then
    OCAMLFIND=ocamlfind
else
    OCAMLFIND=ocamlfind.opt
fi

cd _build/

$OCAMLFIND install benchmark ../META \
    benchmark.mli benchmark.cmi benchmark.cma benchmark.cmx benchmark.cmxa
